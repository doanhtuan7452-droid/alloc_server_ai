services:
  ai_alloc:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai_alloc:latest
    container_name: ai_alloc_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"